import manny3

a=int(input("Enter a number? "))
print(manny3.square(a))

#This demonstartes how if __name__=="__main__" statement can be used as code blocker with respect to
#those files that import the entire codebase and as you can see that the entire manny3 file is not executed.